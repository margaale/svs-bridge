build/
managed_components/
sdkconfig
sdkconfig.old

# Local-only tooling and editor config (machine-specific paths)
.vscode/
.claude/
eim_config.toml

# Version comes from git/GitVersion in CI (written transiently before the build)
version.txt

# Python bytecode (host tests, dev server)
__pycache__/
